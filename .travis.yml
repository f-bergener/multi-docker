sudo: required
services:
  - docker

before_install:
  - docker build -t fbergener/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true fbergener/react-test npm test -- --coverage

after_success:
  - docker build -t fbergener/multi-client ./client
  - docker build -t fbergener/multi-nginx ./nginx
  - docker build -t fbergener/multi-server ./server
  - docker build -t fbergener/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push fbergener/multi-client
  - docker push fbergener/multi-nginx
  - docker push fbergener/multi-server
  - docker push fbergener/multi-worker
